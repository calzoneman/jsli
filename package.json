{
  "name": "@calzoneman/jsli",
  "version": "2.0.0",
  "description": "Logging interface for node.js",
  "main": "index.js",
  "scripts": {
    "build": "babel --source-maps --out-dir lib/ src/",
    "build-watch": "babel --watch --source-maps --out-dir lib/ src/",
    "prepublish": "npm run build",
    "test": "mocha"
  },
  "keywords": [
    "logging"
  ],
  "author": "Calvin Montgomery",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-core": "^6.17.0",
    "babel-preset-es2015": "^6.16.0",
    "mocha": "^3.4.2",
    "sinon": "^2.3.6"
  },
  "babel": {
    "presets": [
      [
        "es2015",
        {
          "loose": true
        }
      ]
    ]
  },
  "dependencies": {
    "sprintf-js": "^1.1.1"
  }
}
